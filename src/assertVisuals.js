var pngjs = require('pngjs');

/**
 * Assert that two visual elements are similar
 * @param imageA
 * @param imageB
 * @param options
 */
function assertVisuals(imageA, imageB, options) {
	
}

module.exports = assertVisuals;
